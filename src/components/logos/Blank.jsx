// import PropTypes from 'prop-types';

// const Blank = ({ color = '#fff', size = '20' }) => {
//   return (
//       fill={color}
//       width={size + 'px'}
//       height={size + 'px'}

//   );
// };

// Blank.propTypes = {
//   color: PropTypes.string.isRequired,
//   size: PropTypes.string.isRequired,
// };

// export default Blank;
